<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Microlearn Quiz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <!-- Navbar -->
    <header class="fixed top-0 left-0 w-full bg-white shadow z-50">
      <nav class="container mx-auto px-5 py-4 flex justify-between items-center">
        <div class="text-xl font-semibold">
          <a href="#" class="text-blue-600 hover:text-blue-800">Microlearn Quiz</a>
        </div>
        <ul class="flex space-x-6 text-sm font-medium">
          <li><a href="#" class="hover:text-blue-600">Home</a></li>
          <li><a href="#" class="hover:text-blue-600">Take Quiz</a></li>
          <li><a href="#" class="hover:text-blue-600">My Scores</a></li>
          <li><a href="#" class="hover:text-blue-600">Profile</a></li>
          <li><a href="#" class="hover:text-red-500">Logout</a></li>
        </ul>
      </nav>
    </header>

    <!-- Main content -->
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-16 py-6">
      <div class="container mx-auto px-5 text-sm flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
        <div class="text-gray-500">
          &copy; <%= Time.current.year %> Microlearn. All rights reserved.
        </div>
        <div class="flex space-x-5">
          <a href="#" class="text-gray-500 hover:text-blue-600">Privacy</a>
          <a href="#" class="text-gray-500 hover:text-blue-600">Terms</a>
          <a href="#" class="text-gray-500 hover:text-blue-600">Contact</a>
        </div>
      </div>
    </footer>
  </body>
</html>
